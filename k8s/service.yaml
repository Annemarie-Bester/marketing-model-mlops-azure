apiVersion: v1
kind: Service
metadata:
  name: bank-marketing-api
  namespace: bank-marketing
spec:
  type: LoadBalancer
  selector:
    app: bank-marketing-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
