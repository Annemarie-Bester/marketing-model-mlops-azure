apiVersion: v1
kind: Service
metadata:
  name: bank-marketing-api
  namespace: bank-marketing-dev
spec:
  type: ClusterIP
  selector:
    app: bank-marketing-api
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
